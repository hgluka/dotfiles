#!/bin/bash
curr_date=`date +"%Y-%m-%d-%T"`
slop=$(slop -f "%x %y %w %h %g %i") || exit 1
read -r X Y W H G ID < <(echo $slop)
ffmpeg -f x11grab -s "$W"x"$H" -framerate 25 -i :0.0+$X,$Y -f alsa -i default ~/Videos/$1
